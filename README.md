# API REST de la Copa Mundial de la FIFA
Laboratorio desarrollado con Node.js, Express, `node:sqlite` y Zod.

## Ejecución
1. Instalar dependencias: `npm install`
2. Poblar BD: `npm run db:populate`
3. Iniciar: `npm run start`